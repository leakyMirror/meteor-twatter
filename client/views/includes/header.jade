template(name="header")
  header.navbar.navbar-default.container(role="navigation")
    .navbar-header
      button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#navigation")
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href="{{pathFor 'stream'}}") Twatter
    .collapse.navbar-collapse#navigation
        ul.nav.navbar-nav.navbar-right
          li
            a(href="{{ pathFor 'stream' }}") Stream
          li
            a(href="{{ pathFor 'userlist' }}") All Twatters
          li.dropdown
            a.dropdown-toggle(href="#", data-toggle="dropdown")
              | {{ username }}
              span.caret
            ul.dropdown-menu
              li
                a#logout-btn(href="#") Logout
