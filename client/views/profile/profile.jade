template(name='profile')
  .container
    .row
      .col-xs-12.text-center
        +gravatar user
        h2 #{ user.username }
          +followButton user
        +profileNumbers user

      +twattList twatts
